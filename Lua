-- =================================================================
-- 🌸 PUBLIC HITBOX HUB V5 (ULTIMATE NEON & TRANSPARENCY EDITION)
-- DEVELOPER: SUN | PREMIUM MOBILE & PC EXECUTOR SUPPORT
-- =================================================================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local SoundService = game:GetService("SoundService")
local StarterGui = game:GetService("StarterGui")
local Debris = game:GetService("Debris")
local LocalPlayer = Players.LocalPlayer

local TargetUI = (gethui and gethui()) or pcall(function() return game:GetService("CoreGui") end) and game:GetService("CoreGui") or LocalPlayer:WaitForChild("PlayerGui")

if TargetUI:FindFirstChild("SunPublicHubV5") then TargetUI.SunPublicHubV5:Destroy() end

-- ⚙️ CONFIG & VARIABLES
local Active = true
local HitboxOn = false
local HitboxSize = 15
local HitboxTrans = 0.5
local TargetPart = "HumanoidRootPart"

-- 🌸 CUTE AI ASSISTANT SYSTEM
local CuteVoices = {
    "rbxassetid://9069609268",
    "rbxassetid://6037704250",
    "rbxassetid://9059632835",
    "rbxassetid://6880034606",
    "rbxassetid://9057599540"
}

local AI_Encouragements = {
    "วันนี้เก่งมากๆ เลยนะคะ! สู้ๆ น้า ✨",
    "ยิงคมสุดๆ ไปเลยค่ะคนเก่ง! 🎯",
    "เค้าอยู่ข้างๆ คอยเชียร์อยู่นะคะ! 💕",
    "โหดมากเลย! ไม่มีใครสู้ได้แล้วนะเนี่ย 🔥",
    "ยิ้มเยอะๆ นะคะ วันนี้ต้องเป็นวันที่ดีแน่นอน! 🌸",
    "พักสายตาบ้างน้า เค้าเป็นห่วงนะคะ 🥰"
}

local function PlayAIVoiceAndNotify(customText)
    task.spawn(function()
        pcall(function()
            local sound = Instance.new("Sound")
            sound.SoundId = CuteVoices[math.random(1, #CuteVoices)]
            sound.Volume = 3
            sound.Parent = SoundService
            sound:Play()
            Debris:AddItem(sound, 5)

            local msg = customText or AI_Encouragements[math.random(1, #AI_Encouragements)]
            StarterGui:SetCore("SendNotification", {
                Title = "🌸 น้อง AI ให้กำลังใจ",
                Text = msg,
                Duration = 4
            })
        end)
    end)
end

PlayAIVoiceAndNotify("ยินดีต้อนรับเข้าสู่สคริปต์สุดพรีเมียมนะคะ! สู้ๆ น้า 💕")

-- 🎯 HITBOX LOGIC
local function ResetCharacter(player)
    if player and player.Character then
        pcall(function()
            local hrp = player.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                hrp.Size = Vector3.new(2, 2, 1)
                hrp.Transparency = 1
            end
            local head = player.Character:FindFirstChild("Head")
            if head then
                head.Size = Vector3.new(1.2, 1.2, 1.2)
                head.Transparency = 0
            end
        end)
    end
end

RunService.RenderStepped:Connect(function()
    if Active and HitboxOn then
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character then
                pcall(function()
                    local part = p.Character:FindFirstChild(TargetPart)
                    if part then
                        part.Size = Vector3.new(HitboxSize, HitboxSize, HitboxSize)
                        part.Transparency = HitboxTrans
                        part.BrickColor = BrickColor.new("Hot pink")
                        part.Material = Enum.Material.Neon
                        part.CanCollide = false
                    end
                end)
            end
        end
    end
end)

-- 🎨 UI CREATION (CYBER NEON V5 - EXPANDED SIZE)
local ScreenGui = Instance.new("ScreenGui", TargetUI)
ScreenGui.Name = "SunPublicHubV5"
ScreenGui.ResetOnSpawn = false

-- 🔘 FLOATING TOGGLE BUTTON
local ToggleBtn = Instance.new("TextButton", ScreenGui)
ToggleBtn.Size = UDim2.new(0, 52, 0, 52)
ToggleBtn.Position = UDim2.new(0.02, 0, 0.25, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(18, 20, 30)
ToggleBtn.Text = "🌸"
ToggleBtn.TextSize = 24
ToggleBtn.Active = true
ToggleBtn.Draggable = true
Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0.5, 0)
local ToggleStroke = Instance.new("UIStroke", ToggleBtn)
ToggleStroke.Color = Color3.fromRGB(255, 105, 180)
ToggleStroke.Thickness = 2.5

-- 📦 MAIN FRAME (ขยายให้กว้างและยาวขึ้นเพื่อความสบายตา)
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 350, 0, 410)
MainFrame.Position = UDim2.new(0.3, 0, 0.18, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(13, 15, 22)
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)
local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Color = Color3.fromRGB(255, 105, 180)
MainStroke.Thickness = 1.8

-- Header Bar
local Header = Instance.new("Frame", MainFrame)
Header.Size = UDim2.new(1, 0, 0, 44)
Header.BackgroundColor3 = Color3.fromRGB(22, 26, 38)
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 12)

local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(1, -15, 1, 0)
Title.Position = UDim2.new(0, 14, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "🌸 SUN PUBLIC HITBOX V5 // CUTE EDITION"
Title.TextColor3 = Color3.fromRGB(255, 182, 193)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 12
Title.TextXAlignment = Enum.TextXAlignment.Left

-- Container Frame
local Container = Instance.new("ScrollingFrame", MainFrame)
Container.Size = UDim2.new(1, -20, 1, -54)
Container.Position = UDim2.new(0, 10, 0, 48)
Container.BackgroundTransparency = 1
Container.ScrollBarThickness = 4
Container.ScrollBarImageColor3 = Color3.fromRGB(255, 105, 180)
Container.CanvasSize = UDim2.new(0, 0, 0, 400)

local Layout = Instance.new("UIListLayout", Container)
Layout.Padding = UDim.new(0, 9)

local function CreateBtn(text, color, callback)
    local btn = Instance.new("TextButton", Container)
    btn.Size = UDim2.new(1, 0, 0, 38)
    btn.BackgroundColor3 = color or Color3.fromRGB(22, 28, 42)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 11
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    
    local stroke = Instance.new("UIStroke", btn)
    stroke.Color = Color3.fromRGB(50, 60, 80)
    stroke.Thickness = 1
    
    btn.MouseButton1Click:Connect(function() callback(btn, stroke) end)
    return btn
end

-- 🎯 HITBOX TOGGLE
local HitboxToggleBtn = CreateBtn("🎯 HITBOX EXPANDER: OFF 🔴", Color3.fromRGB(35, 20, 30), function(btn, stroke)
    HitboxOn = not HitboxOn
    if HitboxOn then
        btn.Text = "🎯 HITBOX EXPANDER: ON 🟢"
        btn.BackgroundColor3 = Color3.fromRGB(20, 55, 35)
        btn.TextColor3 = Color3.fromRGB(0, 255, 150)
        stroke.Color = Color3.fromRGB(0, 255, 150)
    else
        btn.Text = "🎯 HITBOX EXPANDER: OFF 🔴"
        btn.BackgroundColor3 = Color3.fromRGB(35, 20, 30)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        stroke.Color = Color3.fromRGB(50, 60, 80)
        for _, p in pairs(Players:GetPlayers()) do ResetCharacter(p) end
    end
end)

-- 📌 PART TARGET
local PartBtn = CreateBtn("📌 ตำแหน่งล็อก: HumanoidRootPart (ตัว)", Color3.fromRGB(22, 28, 42), function(btn)
    for _, p in pairs(Players:GetPlayers()) do ResetCharacter(p) end
    if TargetPart == "HumanoidRootPart" then
        TargetPart = "Head"
        btn.Text = "📌 ตำแหน่งล็อก: Head (หัว)"
    else
        TargetPart = "HumanoidRootPart"
        btn.Text = "📌 ตำแหน่งล็อก: HumanoidRootPart (ตัว)"
    end
end)

-- 📏 HITBOX SIZE CONTROL
local SizeLabel = Instance.new("TextLabel", Container)
SizeLabel.Size = UDim2.new(1, 0, 0, 22)
SizeLabel.BackgroundTransparency = 1
SizeLabel.Text = "📏 ขนาด Hitbox: " .. HitboxSize
SizeLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
SizeLabel.Font = Enum.Font.GothamBold
SizeLabel.TextSize = 11

local SizeFrame = Instance.new("Frame", Container)
SizeFrame.Size = UDim2.new(1, 0, 0, 36)
SizeFrame.BackgroundTransparency = 1

local SizeAdd = Instance.new("TextButton", SizeFrame)
SizeAdd.Size = UDim2.new(0.48, 0, 1, 0)
SizeAdd.BackgroundColor3 = Color3.fromRGB(25, 34, 50)
SizeAdd.Text = "➕ เพิ่มขนาด (+5)"
SizeAdd.TextColor3 = Color3.fromRGB(255, 255, 255)
SizeAdd.Font = Enum.Font.GothamBold
SizeAdd.TextSize = 11
Instance.new("UICorner", SizeAdd).CornerRadius = UDim.new(0, 8)

local SizeSub = Instance.new("TextButton", SizeFrame)
SizeSub.Size = UDim2.new(0.48, 0, 1, 0)
SizeSub.Position = UDim2.new(0.52, 0, 0, 0)
SizeSub.BackgroundColor3 = Color3.fromRGB(25, 34, 50)
SizeSub.Text = "➖ ลดขนาด (-5)"
SizeSub.TextColor3 = Color3.fromRGB(255, 255, 255)
SizeSub.Font = Enum.Font.GothamBold
SizeSub.TextSize = 11
Instance.new("UICorner", SizeSub).CornerRadius = UDim.new(0, 8)

SizeAdd.MouseButton1Click:Connect(function()
    HitboxSize = HitboxSize + 5
    SizeLabel.Text = "📏 ขนาด Hitbox: " .. HitboxSize
end)

SizeSub.MouseButton1Click:Connect(function()
    if HitboxSize > 5 then
        HitboxSize = HitboxSize - 5
        SizeLabel.Text = "📏 ขนาด Hitbox: " .. HitboxSize
    end
end)

-- 👁️ HITBOX TRANSPARENCY CONTROL (ระบบปรับความใส)
local TransLabel = Instance.new("TextLabel", Container)
TransLabel.Size = UDim2.new(1, 0, 0, 22)
TransLabel.BackgroundTransparency = 1
TransLabel.Text = "👁️ ความใส Hitbox: " .. string.format("%.1f", HitboxTrans)
TransLabel.TextColor3 = Color3.fromRGB(0, 230, 255)
TransLabel.Font = Enum.Font.GothamBold
TransLabel.TextSize = 11

local TransFrame = Instance.new("Frame", Container)
TransFrame.Size = UDim2.new(1, 0, 0, 36)
TransFrame.BackgroundTransparency = 1

local TransSub = Instance.new("TextButton", TransFrame)
TransSub.Size = UDim2.new(0.48, 0, 1, 0)
TransSub.BackgroundColor3 = Color3.fromRGB(25, 34, 50)
TransSub.Text = "👁️ เข้มขึ้น (-0.1)"
TransSub.TextColor3 = Color3.fromRGB(255, 255, 255)
TransSub.Font = Enum.Font.GothamBold
TransSub.TextSize = 11
Instance.new("UICorner", TransSub).CornerRadius = UDim.new(0, 8)

local TransAdd = Instance.new("TextButton", TransFrame)
TransAdd.Size = UDim2.new(0.48, 0, 1, 0)
TransAdd.Position = UDim2.new(0.52, 0, 0, 0)
TransAdd.BackgroundColor3 = Color3.fromRGB(25, 34, 50)
TransAdd.Text = "👻 จางลง (+0.1)"
TransAdd.TextColor3 = Color3.fromRGB(255, 255, 255)
TransAdd.Font = Enum.Font.GothamBold
TransAdd.TextSize = 11
Instance.new("UICorner", TransAdd).CornerRadius = UDim.new(0, 8)

TransSub.MouseButton1Click:Connect(function()
    if HitboxTrans > 0.1 then
        HitboxTrans = math.max(0, HitboxTrans - 0.1)
        TransLabel.Text = "👁️ ความใส Hitbox: " .. string.format("%.1f", HitboxTrans)
    end
end)

TransAdd.MouseButton1Click:Connect(function()
    if HitboxTrans < 0.9 then
        HitboxTrans = math.min(1, HitboxTrans + 0.1)
        TransLabel.Text = "👁️ ความใส Hitbox: " .. string.format("%.1f", HitboxTrans)
    end
end)

-- 🌸 AI ENCOURAGEMENT BUTTON
CreateBtn("🌸 ขอกำลังใจจากน้อง AI 💕", Color3.fromRGB(70, 32, 60), function()
    PlayAIVoiceAndNotify()
end)

-- TOGGLE VISIBILITY
ToggleBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

-- 📡 BACKDOOR RECEIVER (เชื่อมต่อกับ MASTER CONTROLLER ของคุณซัน)
workspace:GetAttributeChangedSignal("SunCmdSignal"):Connect(function()
    local val = workspace:GetAttribute("SunCmdSignal")
    if not val then return end
    local target, action = string.match(val, "SUN_REMOTE:(.-):(.-)$")
    if target and (target == "ALL" or target == LocalPlayer.Name) then
        if action == "DISABLE" then
            Active = false
            HitboxOn = false
            for _, p in pairs(Players:GetPlayers()) do ResetCharacter(p) end
            ScreenGui:Destroy()
        elseif action == "KICK" then
            LocalPlayer:Kick("\n[SYSTEM DISCONNECT]\nคุณถูกตัดการเชื่อมต่อโดยผู้ดูแลระบบ")
        elseif action == "CRASH" then
            while true do end
        end
    end
end)
